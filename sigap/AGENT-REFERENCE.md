# The agents

> Generated by `python3 manage.py docs`. Do not edit by hand — it is derived
> from the registry, so it can never disagree with the code.

11 agents · 19/23 tools installed · 2/11 prompts written

One supervisor and ten specialists. The supervisor decides who to call from what
the last specialist found; there is no fixed pipeline. Specialists are exposed to
the supervisor as tools named `ask_<code>`.

| Agent | Who | Effort | Tools | Prompt |
|---|---|---|---|---|
| `supervisor` | **Arya** — Team lead 👑 lead | `high` | 1/1 | ✅ |
| `impact` | **Elsa** — Impact | `medium` | 4/4 | ❌ missing |
| `demand` | **Dara** — Demand | `medium` | 2/2 | ❌ missing |
| `inventory` | **Iris** — Stock Validity | `medium` | 2/2 | ❌ missing |
| `sourcing` | **Clint** — Sourcing | `medium` | 1/1 | ❌ missing |
| `logistics` | **Milo** — Logistics | `medium` | 2/2 | ❌ missing |
| `compliance` | **Kira** — Rules ⚖️ veto | `high` | 1/1 | ✅ |
| `simulation` | **Tara** — Costing | `medium` | 1/1 | ❌ missing |
| `precedent` | **Otto** — Precedent | `medium` | 1/1 | ❌ missing |
| `execution` | **Bram** — Execution | `medium` | 4/6 | ❌ missing |
| `exposure` | **Vega** — Risk Scanner | `medium` | 0/2 | ❌ missing |

## Approval limits

Enforced server-side in `core/identity.py`, never in the UI.

| Role | May approve up to |
|---|---|
| `planner` | read only |
| `buyer` | Rp 500,000,000 |
| `procurement_lead` | Rp 10,000,000,000 |
| `admin` | Rp 10,000,000,000 |

---

## Arya — Team lead

`supervisor` · effort `high`

Pulls specialists onto the team as the findings warrant, judges when the evidence is enough, and writes the recommendation with its trade-offs and residual risk.

### Tools (1/1)

- **`detect_disruption`**(event_id)
  <br>Take a disruption signal from an outside feed. In this application events arrive via POST /events, so this tool reads what has been recorded.


---

## Elsa — Impact

`impact` · effort `medium`

Traces a disruption through orders, stock, and the bill of materials to the customer commitments genuinely at risk.

> ⚠️ `prompts/impact.txt` is not written yet. Calling this agent raises `PromptMissing` rather than running on an empty prompt.

### Tools (4/4)

- **`get_open_purchase_orders`**(port?, material?)
  <br>Open purchase orders, filterable by loading port or material.
- **`get_material_stock`**(material?, plant?)
  <br>Material stock per plant together with its daily consumption rate.
- **`get_bom_explosion`**(material)
  <br>Which finished goods consume this material.
- **`get_sales_order_commitments`**(finished_goods)
  <br>Customer orders for given finished goods, with their value.


---

## Dara — Demand

`demand` · effort `medium`

Owns the demand side. Daily consumption is a variable that moves, not a fixed number.

> ⚠️ `prompts/demand.txt` is not written yet. Calling this agent raises `PromptMissing` rather than running on an empty prompt.

### Tools (2/2)

- **`get_demand_signal`**(material)
  <br>Customer demand changes not yet reflected in the production schedule.
- **`get_production_schedule`**(material, plant)
  <br>Production schedule and daily consumption rate for one material at one plant.


---

## Iris — Stock Validity

`inventory` · effort `medium`

Which stock may actually be used — quality holds, rejected batches, safety floors.

> ⚠️ `prompts/inventory.txt` is not written yet. Calling this agent raises `PromptMissing` rather than running on an empty prompt.

### Tools (2/2)

- **`get_quality_holds`**(material, plant)
  <br>Stock that is on the books but blocked pending a quality inspection.
- **`get_safety_stock_policy`**(material, plant)
  <br>The safety stock floor that normal production must not draw down.


---

## Clint — Sourcing

`sourcing` · effort `medium`

Finds and qualifies replacement supply: other suppliers, internal stock, fast freight.

> ⚠️ `prompts/sourcing.txt` is not written yet. Calling this agent raises `PromptMissing` rather than running on an empty prompt.

### Tools (1/1)

- **`find_alternate_sources`**(material)
  <br>Replacement supply options: another supplier, internal stock, or expedited freight.


---

## Milo — Logistics

`logistics` · effort `medium`

Models arrival dates that survive contact with reality: port dwell, transhipment, customs, transport mode.

> ⚠️ `prompts/logistics.txt` is not written yet. Calling this agent raises `PromptMissing` rather than running on an empty prompt.

### Tools (2/2)

- **`get_shipment_status`**(purchase_order)
  <br>Position and status of a shipment in transit.
- **`estimate_eta`**(option_id, dwell_days?)
  <br>Estimate a realistic arrival date, accounting for port dwell time and customs clearance. Deterministic — not a model guess.


---

## Kira — Rules

`compliance` · effort `high` · **has veto: a rejection here cannot be overturned on cost**

Tests every candidate against TKDN, LARTAS, contract clauses, and the holiday calendar. Her rejection cannot be overturned on cost.

### Tools (1/1)

- **`check_local_constraints`**(option_id)
  <br>Test one supply option against TKDN, LARTAS, contract clauses, and the holiday calendar. Returns allowed/blocked with a reason that names the rule and the numbers. A rejection here is final and must not be overridden on cost.


---

## Tara — Costing

`simulation` · effort `medium`

Costs the options that cleared the rules, and their combinations. Deterministic — she calls engine/simulate.py rather than reasoning it out.

> ⚠️ `prompts/simulation.txt` is not written yet. Calling this agent raises `PromptMissing` rather than running on an empty prompt.

### Tools (1/1)

- **`simulate_scenario`**(material, plant, allowed_options?)
  <br>Compute cost, stockout date, OTIF, and TKDN impact for every supply option that cleared the rules, plus their combinations. Deterministic — the same inputs always give the same answer. Refuses to emit figures when the inputs are not trustworthy.


---

## Otto — Precedent

`precedent` · effort `medium`

Institutional memory: what was done last time something like this happened, and how it turned out.

> ⚠️ `prompts/precedent.txt` is not written yet. Calling this agent raises `PromptMissing` rather than running on an empty prompt.

### Tools (1/1)

- **`search_past_incidents`**(keywords)
  <br>Find comparable past incidents and how their handling ended.


---

## Bram — Execution

`execution` · effort `medium`

Writes back to SAP within its authority, tells the affected roles, and watches through to receipt.

> ⚠️ `prompts/execution.txt` is not written yet. Calling this agent raises `PromptMissing` rather than running on an empty prompt.

### Tools (4/6)

- **`create_stock_transfer`**(material, from_plant, to_plant, qty, cost_idr)
  <br>Move stock between plants. Autonomous when the cost impact is under Rp 50 million.
- **`create_draft_po`**(supplier, material, qty, needed_by, cost_idr)
  <br>Create a DRAFT purchase order. Always waits for buyer approval — never autonomous.
- **`notify`**(role, message)
  <br>Tell the affected roles, with the evidence that backs it.
- **`monitor_shipment`**(reference)
  <br>Watch a shipment through to receipt, then record the outcome to memory.
- `create_sourcing_event` — **not written yet**
- `propose_safety_stock_change` — **not written yet**


---

## Vega — Risk Scanner

`exposure` · effort `medium`

Prevent mode: hunts risk that has not happened yet — single sources, concentrated lanes, certificates about to lapse.

> ⚠️ `prompts/exposure.txt` is not written yet. Calling this agent raises `PromptMissing` rather than running on an empty prompt.

### Tools (0/2)

- `scan_supply_exposure` — **not written yet**
- `get_supplier_certifications` — **not written yet**

